      module varelimin

!     elimin.for variables (shared between wrcont.for and etl.for)

      real*8, allocatable(:, :, :) :: BX

      real*8, allocatable(:, :) :: WX

      end module varelimin
